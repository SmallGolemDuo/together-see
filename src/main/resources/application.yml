spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/together_see?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: tjddnr12
  thymeleaf:
    cache: false

  # spring data jpa
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop

  # swagger setting
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

kakao:
  api:
    endpoint: "https://kauth.kakao.com"
    client-id: "16193d7b26557863922f0139722f8829"
    redirect-url: "http://localhost:8080/oauth/redirect"